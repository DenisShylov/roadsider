import { Box, Container } from '@mui/material';
import React from 'react';

const Companies = () => {
  return (
    <Container>
      <Box>Companies</Box>
    </Container>
  );
};

export default Companies;
